<?xml version="1.0" encoding="UTF-8"?>
<shared:SampleContentPage xmlns="http://xamarin.com/schemas/2014/forms"
                          xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                          xmlns:shared="clr-namespace:Sample;assembly=Sample.Shared"
                          xmlns:sample="clr-namespace:Sample"
                          x:DataType="sample:BadgeViewModel"
                          x:Class="Sample.BadgePage"
                          Title="Badges">

    <ContentPage.BindingContext>
        <sample:BadgeViewModel />
    </ContentPage.BindingContext>

    <ContentPage.Content>
        <TableView>
            <TableRoot>
                <TableSection>
                    <EntryCell Label="Badge"
                               Text="{Binding Badge}"
                               Keyboard="Numeric" />
                </TableSection>
                <TableSection>
                    <TextCell Text="Clear" Command="{Binding Clear}" />
                </TableSection>
            </TableRoot>
        </TableView>
    </ContentPage.Content>
</shared:SampleContentPage>
